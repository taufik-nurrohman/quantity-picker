extends ../_index.pug

block state
  - const scripts = ['../index.min.js']
  - const styles = ['../index.min.css']
  - const title = 'Quantity Picker'

block script
  script
    | const picker = new QuantityPicker(document.querySelector('input'));

block style
  style
    | .quantity-picker__text {
    |   min-width: 3em;
    | }

block content
  main
    p: input(autofocus id='test' min='0' step='1' type='number' value='1')
    p #[label(for='test' style={'text-decoration': 'underline'}) Click here] to focus to the quantity picker!