extends _index.pug

block state
  - const scripts = ['index.min.js']
  - const styles = ['index.min.css']
  - const title = 'Number Picker'

block script
  script
    | const picker = new QuantityPicker(document.querySelector('input'));

block style
  style
    | .quantity-picker__text {
    |   min-width: 1.5em;
    | }

block content
  main
    p: input(min='0' step='1' type='number')